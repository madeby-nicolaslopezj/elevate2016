orion.dictionary.addDefinition('logo', 'footer', orion.attribute('image', {
  label: 'Logo'
}));

orion.dictionary.addDefinition('text', 'footer', {
  type: String,
  label: "Text",
  optional: true,
});

orion.dictionary.addDefinition('image1', 'footer', orion.attribute('image', {
  label: 'Imagen 1'
}));

orion.dictionary.addDefinition('image2', 'footer', orion.attribute('image', {
  label: 'Imagen 2'
}));
